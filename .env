# Deployed dev backend — switch back to this once its DB credentials are fixed
# and today's backend changes are deployed there.
#VITE_API_BASE_URL=https://api-dev.erlume.com.kw/api
VITE_API_BASE_URL=http://localhost:3000/api
